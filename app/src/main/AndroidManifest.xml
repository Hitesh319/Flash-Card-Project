<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FlashcardApp">

        <!-- Splash Screen / Flash Screen Activity -->
        <activity
            android:name=".FlashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Home Screen Activity -->
        <activity
            android:name=".HomeScreenActivity"
            android:exported="true" />

        <!-- Flashcard Edit Activity -->
        <activity
            android:name=".FlashcardEditActivity"
            android:exported="true" />

        <!-- Flashcard View Activity -->
        <activity
            android:name=".FlashcardViewActivity"
            android:exported="true" />

    </application>
    <uses-permission android:name="android.permission.INTERNET" />


</manifest>
